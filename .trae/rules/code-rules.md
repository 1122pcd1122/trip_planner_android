---
alwaysApply: false
description: 生成 Android 架构设计与代码时，遵循 MVVM 分层解耦架构，优先使用 Jetpack 组件；
---
## 架构规范

采用MVVM架构，分层清晰（视图层/ viewModel层/模型层）；禁止层间直接调用，通过接口解耦；优先使用Jetpack组件，提升稳定性。

## 代码规范

命名规范：类名帕斯卡、方法/变量名驼峰；注释简洁明了，关键逻辑必加；代码格式统一，禁止冗余代码；异常统一捕获，避免崩溃。